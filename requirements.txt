# Google Gemini API SDK
google-generativeai>=0.4.0

# Environment variables management
python-dotenv>=1.0.0

# Structured data and schemas (critical for Structured Outputs with Gemini)
pydantic>=2.0.0

# Web server dependencies (to build and test backend endpoints/services)
fastapi>=0.100.0
uvicorn>=0.22.0

# Data utilities (used for vector calculations in RAG)
numpy>=1.22.0

# Testing
pytest>=7.0.0
